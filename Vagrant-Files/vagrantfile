# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
    config.vm.box = "ubuntu/bionic64" # Ubuntu 18.04 LTS
    config.vm.provider "virtualbox" do |vb|
      vb.name = "vagrant_ubuntu_server"
    end
  end
  